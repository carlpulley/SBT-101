package com.cakesolutions

import org.scalatest.FlatSpec

class FirstSpec extends FlatSpec {
  
  "A simple test" should "always succeed" in {
    assert(true, "How could this test ever fail!!")
  }

}
